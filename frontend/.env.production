# Variáveis de ambiente para produção (Easypanel)
# Este arquivo é usado automaticamente pelo Vite quando NODE_ENV=production

# URL da API Backend (WebSocket)
# Use wss:// para conexões seguras (HTTPS)
VITE_API_URL=wss://rtp-api.zapchatbr.com
